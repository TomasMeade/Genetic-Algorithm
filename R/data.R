#' Large testing dataset
#'
#' Dataset made for testing \code{select} on a dataset with
#' a limited number of variables. The response variable is in
#' the 14th column and is generated using \code{x5}, \code{x8}, and \code{x13}.
#'
#' @usage data(large_testing_data)
#' @format A data frame with 1000 rows and 14 variables.
"large_testing_data"

#' Huge testing dataset
#'
#' List of a dataset and the predictor variables used to generate the
#' response variable in the dataset. The dataset was made for testing \code{select}
#' on a dataset with a large number of variables. The response variable is in
#' the 51st column and is generated using \code{X15}, \code{X26}, and \code{X45}.
#'
#' @usage data(huge_testing_data)
#' @format A list with a data frame and a numeric vector. The data frame has 1000 rows
#' and 51 variables. The vector has length 3.
"huge_testing_data"
